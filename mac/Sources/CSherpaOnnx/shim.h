// The header comes from vendor/, fetched by scripts/setup.sh alongside the
// static libraries it describes, so the two can never be from different
// sherpa-onnx versions.
#include "../../vendor/include/sherpa-onnx/c-api/c-api.h"
